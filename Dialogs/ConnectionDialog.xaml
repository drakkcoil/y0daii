<Window x:Class="Y0daiiIRC.ConnectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Connect to IRC Server" 
        Height="500" 
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8F9FA"
        TextElement.Foreground="#323130"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- Office 365 Color Scheme -->
        <SolidColorBrush x:Key="OfficeBlue" Color="#0078D4"/>
        <SolidColorBrush x:Key="OfficeBlueHover" Color="#106EBE"/>
        <SolidColorBrush x:Key="OfficeBluePressed" Color="#005A9E"/>
        <SolidColorBrush x:Key="OfficeGray50" Color="#F8F9FA"/>
        <SolidColorBrush x:Key="OfficeGray100" Color="#F3F2F1"/>
        <SolidColorBrush x:Key="OfficeGray200" Color="#EDEBE9"/>
        <SolidColorBrush x:Key="OfficeGray300" Color="#E1DFDD"/>
        <SolidColorBrush x:Key="OfficeGray400" Color="#C8C6C4"/>
        <SolidColorBrush x:Key="OfficeGray500" Color="#A19F9D"/>
        <SolidColorBrush x:Key="OfficeGray600" Color="#8A8886"/>
        <SolidColorBrush x:Key="OfficeGray700" Color="#605E5C"/>
        <SolidColorBrush x:Key="OfficeGray800" Color="#484644"/>
        <SolidColorBrush x:Key="OfficeGray900" Color="#323130"/>

        <!-- Button Styles -->
        <Style x:Key="OfficeButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource OfficeBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="2"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource OfficeBlueHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource OfficeBluePressed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OfficeSecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource OfficeBlue}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource OfficeGray300}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource OfficeGray100}"/>
                                <Setter Property="BorderBrush" Value="{StaticResource OfficeBlue}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource OfficeGray200}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource OfficeGray200}" BorderThickness="0,0,0,1">
            <Grid Height="48">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0">
                    <materialDesign:PackIcon Kind="ServerNetwork" Width="20" Height="20" VerticalAlignment="Center" Foreground="{StaticResource OfficeBlue}" Margin="0,0,8,0"/>
                    <TextBlock Text="Connect to IRC Server" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{StaticResource OfficeGray900}"/>
                </StackPanel>
                
                <Button Grid.Column="1" 
                        Width="32" 
                        Height="32" 
                        Background="Transparent" 
                        BorderThickness="0" 
                        Click="CloseButton_Click"
                        Margin="16,0">
                    <materialDesign:PackIcon Kind="Close" Width="16" Height="16" Foreground="{StaticResource OfficeGray700}"/>
                </Button>
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Background="White">
            <StackPanel Margin="32">
                <!-- Server Information -->
                <Border Background="{StaticResource OfficeGray50}" Padding="16" CornerRadius="8" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Server Information" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource OfficeGray900}" Margin="0,0,0,12"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Server:" Width="100" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <TextBox x:Name="ServerTextBox" Width="300" Text="irc.libera.chat" Padding="8"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Port:" Width="100" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <TextBox x:Name="PortTextBox" Width="100" Text="6667" Padding="8"/>
                            <CheckBox x:Name="UseSslCheckBox" Content="Use SSL" Margin="16,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- User Information -->
                <Border Background="{StaticResource OfficeGray50}" Padding="16" CornerRadius="8" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="User Information" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource OfficeGray900}" Margin="0,0,0,12"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Nickname:" Width="100" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <TextBox x:Name="NicknameTextBox" Width="200" Text="Y0daiiUser" Padding="8"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Username:" Width="100" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <TextBox x:Name="UsernameTextBox" Width="200" Text="y0daii" Padding="8"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Real Name:" Width="100" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <TextBox x:Name="RealNameTextBox" Width="300" Text="y0daii IRC User" Padding="8"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Ident Server -->
                <Border Background="{StaticResource OfficeGray50}" Padding="16" CornerRadius="8" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Ident Server (Optional)" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource OfficeGray900}" Margin="0,0,0,12"/>
                        
                        <CheckBox x:Name="UseIdentCheckBox" Content="Use Ident Server" Margin="0,0,0,8" FontWeight="SemiBold"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Ident Server:" Width="100" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <TextBox x:Name="IdentServerTextBox" Width="200" Text="127.0.0.1" Padding="8"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Ident Port:" Width="100" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <TextBox x:Name="IdentPortTextBox" Width="100" Text="113" Padding="8"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource OfficeGray200}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right" 
                        Margin="16,12">
                <Button x:Name="CancelButton"
                        Content="Cancel"
                        Style="{StaticResource OfficeSecondaryButtonStyle}"
                        Click="CancelButton_Click"
                        Margin="0,0,8,0"/>
                <Button x:Name="ConnectButton"
                        Content="Connect"
                        Style="{StaticResource OfficeButtonStyle}"
                        Click="ConnectButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>